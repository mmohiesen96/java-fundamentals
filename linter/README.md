### Linter will specify the line where you miss a semicolon unless some cases occur :
    - ends with an opening curly braces or closing one .
    - Contains if or else key words .
    - new Features will be extended to that .